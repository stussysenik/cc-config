Run the Claude Code daily summary in compact mode for a quick glance.

Execute this command:
```bash
python3 ~/Desktop/cc-config/summary.py --compact
```

Display the output to the user. This is a condensed view of today's activity.
